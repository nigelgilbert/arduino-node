## Getting setup

`git clone https://github.com/nigelgilbert/pomodoro.git`

##### OSX:
`rm -rf .git`

##### Windows:

`del /s /q .svn`

`rmdir /s /q .svn`

`npm install`

`npm install electron -g`

`npm run start`
 